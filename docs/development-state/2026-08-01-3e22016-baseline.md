# Development baseline: 2026-08-01 / `3e22016`

This non-normative record describes operator-controlled development checks for
`3e22016cc171cca9bd48f4231b8118287cf8bece`. It is not independent evaluation,
protected custody, witnessing, governance approval, certification, or promotion.

## GitHub state

GitHub authentication and logs were accessible. Eight workflows were current for the
commit: seven completed successfully and the mixed `CI` workflow failed. The failed
job was `quality (3.13)` at `ruff format --check .`; it identified three RAVEL 0.5
tool sources requiring formatting. The other Python quality matrix jobs were then
cancelled. The release-candidate, installed-wheel, and cross-implementation jobs
completed successfully. The bounded 50,147-byte failing-job log has SHA-256
`9451242cbcbdcd7ea4c5de212eb8711a0e5f786ff795c1db91d13089ab173646`.

This CI state is current only for the starting commit. It says nothing about later
unpushed local commits.

## Local state

The supported editable development installation succeeded in an isolated ignored
environment. `make check` reproduced the formatting failure immediately. The RAVEL
0.5 integrity check, negative evidence test, manifest mutation test, and sanitizer
matrix all succeeded.

The RAVEL results establish reproduction integrity only. They do not change the
frozen RAVEL 0.5 development result (`FAIL`), formal MNCS/MNCDS statuses (`UNKNOWN`),
or unauthorized promotion state.

The sibling Forge CLI validated the checked-in configuration. The baseline declared
no providers, verifiers, or required capabilities, so provider availability was
`UNKNOWN`; an empty blocker set meant only that no capability policy existed.

The repository-wide Joern snapshot timed out at 240 seconds. A real focused CPG
snapshot of `src/mncs_validator` completed with Joern 4.0.583 and captured the
assurance dispatch, dependency propagation, revalidation, lifecycle, and control
dependencies. Python frontend iterator nodes include explicit `UNKNOWN` nodes, which
limits exceptional-edge conclusions.

Exact run metadata, executable identities, command output digests, timestamps,
excerpts, limitations, and workflow URLs are in the adjacent JSON record.
