# Post-change development state — 2026-08-01

This non-normative record covers the clean evaluated commit
`c8322e21ded45114b2278791b2ee7b804d3fe1c2`, based on starting commit
`3e22016cc171cca9bd48f4231b8118287cf8bece`.

It is operator-controlled development evidence. It is not independent
evaluation, protected custody, witnessing, governance approval, certification,
or promotion. Workflow success is not an MNCS or MNCDS conformance result.

## CI comparison

The starting commit had eight current GitHub Actions runs: seven succeeded and
the mixed `CI` workflow failed when Ruff requested formatting of three
source-identity-frozen RAVEL 0.5 tools. The exact baseline record retains the
run/job inventory and bounded log digest.

The evaluated commit is local and unpushed. A commit-filtered GitHub query
returned no runs, so post-change GitHub CI is `UNKNOWN`, not passing. Local
verification is recorded separately below.

## Local result

After refreshing the isolated `.venv`, the final surface passed:

- Ruff format and lint, strict mypy, and `git diff --check`;
- 167 unfiltered pytest tests;
- `make core-check`, package build, installed schemas/examples/corpora,
  interoperability, release-candidate comparison, recursive study, and docs;
- Python/Rust category and required issue-code agreement on 74/74 vectors;
- Cargo format, tests, and Clippy with warnings denied;
- frozen RAVEL 0.5 reproduction, negative/mutation campaigns,
  manifest-negative campaign, and ASan/UBSan reproduction;
- 14 Forge integration/security/configuration tests and all four explicit
  representative verifier runs;
- seven RAVEL 0.6 preregistration/identity/partition tests; and
- the offline release-gap issue-map validator.

The machine-readable companion records exact commands, environment-key names,
exit statuses, bounded durations, output sizes and SHA-256 identities.

## Preserved nonzero observations

Nonzero intermediate results were not erased:

1. An unactivated host `make core-check` could not import Hypothesis. The
   isolated environment was refreshed and its `PATH` made explicit.
2. The first isolated core run found that Rust omitted the
   `rollback-not-tested` issue code after another required uncertainty had
   already lowered the result. The category remained correctly `UNKNOWN`.
   The reporting defect was fixed and regression tested without changing
   precedence or normative text.
3. Ruff then caught an unformatted new regression test; formatting was applied
   before the final pass.
4. Two timing-wrapper attempts treated a `PATH=...` assignment as an
   executable and returned 127. The actual targets were rerun correctly.

## Implementation and coverage

The Python assurance validator is now a compatibility façade over narrow model,
status, contract, graph, freshness, revalidation, lifecycle, records, and
dispatch modules. Required material-change impact now includes every transitive
upstream required dependent. This corrects implementation behavior to existing
MNCS 0.3-rc.1 sections 7–8; normative text and RC identifiers did not change.

The Rust consumer now accepts bounded user records, parses RFC 3339 offsets,
implements current graph/revalidation/lifecycle and MNCDS subsets, and reports
all concurrent required limitations. General MNCS 0.2 package/archive and
DSSE/Ed25519 trust-policy validation remain explicitly unsupported. Separate
Rust code does not establish independent operation or organizational
independence.

## Forge

The project-owned registry declares four required Provider Protocol methods:
`evidence-change-impact`, `artifact-manifest-identity`,
`mncs-assurance-graph-impact`, and `mncs-record-dispatch`. Configuration,
explicit capability probe, blocker evaluation, and verifier discovery pass;
the current blocker set is empty at the capability-availability layer.
Representative valid bounded requests returned narrow `PASS` results and
created local ledger records.

These are same-project development results. They cannot establish global
semantic independence, artifact truth, MNCS/MNCDS conformance, independent
operation, protected custody, or promotion.

## RAVEL boundary

RAVEL 0.4 and 0.5 key source/evidence identities were checked against fixed
SHA-256 values. The historical root `Makefile` is restored byte-for-byte and
new development targets live in `GNUmakefile`, preserving the 0.5 manifest
binding.

RAVEL 0.5 execution integrity reproduces as `PASS`; its frozen development
result remains `FAIL`, formal MNCS/MNCDS statuses remain `UNKNOWN`, and
promotion remains unauthorized. RAVEL 0.6 is preregistered before
implementation with distinct development, selection, retention,
transition-retention, planning, and future-final identities. It has no
candidate or final seed material. All implementation, evaluation, custody,
independence, MNCS, and MNCDS states remain `UNKNOWN`; promotion is `false`.

## Joern

Real Joern 4.0.583 focused Python snapshots were created before and after the
validator refactor and compared without snapshot warnings. Post queries show
explicit required-only upstream graph closure, separate revalidation
sufficiency controls, and assurance-level rejection of incomplete impact.

The installed bridge does not support Rust and rejected both requested Rust
snapshots. No Rust CPG conclusion is claimed. Python frontend iterator nodes
retain Joern `UNKNOWN` representation, and the initial whole-repository parse
timed out at 240 seconds.

## Issues and remaining blockers

Gap fingerprints map exactly once to issues
[#34](https://github.com/epi13/machine-native-complexity-standard/issues/34)
through
[#39](https://github.com/epi13/machine-native-complexity-standard/issues/39).
Freshness and change/revalidation are newly covered. Issue #36 retains the
local inherited package/attestation implementation gap. The other issues retain
governance, independent review, external evaluation/custody, security/privacy,
and release-authority blockers that local code cannot manufacture.
