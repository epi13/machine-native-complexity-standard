# Exception-driven repair

Runtime cases passed, but a structural provider found one return before range
validation. One compact witness was supplied. The regenerated candidate received a
new hash, the full suite was rerun, and the invariant passed. `repair.json` records
that structural analysis found a path runtime tests missed.
